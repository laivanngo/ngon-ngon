# Core
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10             # WHY: Alembic chạy sync, CẦN sync driver. asyncpg chỉ cho app runtime.
alembic==1.14.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1                      # PHẢI dùng 4.0.x — bcrypt 4.1+ thay đổi API, passlib 1.7.4 crash

# HTTP client (Zalo integration + test client)
httpx==0.28.1

# Utilities
python-multipart==0.0.20

# Testing (also needed for seed script check)
pytest==8.3.4
pytest-asyncio==0.25.0
aiosqlite==0.21.0
